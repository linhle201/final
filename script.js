const burger = document.querySelector('.burger');
const navLinks = document.querySelector('.nav-links');

burger.addEventListener('click', () => {
  navLinks.classList.toggle('show-menu');
});

// Function to handle the 'More about me' and 'Read More' buttons
function openBox() {
  // Add your logic here for what should happen when these buttons are clicked
  // For example, displaying more information or redirecting to a different page
}